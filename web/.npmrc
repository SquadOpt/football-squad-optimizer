# A declared engine that only warns is a declaration nobody reads. `npm ci` on the wrong
# Node must refuse, because the failure it prevents does not look like a version problem:
# on Node 26 the jsdom environment does not come up, `window.localStorage` is undefined,
# and 118 of this suite's tests fail with errors that name neither Node nor jsdom.
engine-strict=true
